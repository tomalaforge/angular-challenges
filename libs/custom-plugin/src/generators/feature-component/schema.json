{
  "$schema": "http://json-schema.org/schema",
  "$id": "FeatureComponent",
  "title": "",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The component name.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?"
    },
    "createService": {
      "type": "boolean",
      "description": "The http service related to component store.",
      "default": true
    },
    "inlineTemplate": {
      "type": "boolean",
      "description": "A flag indicating if component template will be placed inline or within an external html file.",
      "default": false
    }
  },
  "required": ["name"]
}
